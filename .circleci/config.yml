version: 2.1

jobs:
  deploy-over-ssh:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Deploy Over SSH
          command: 'echo Hello, World!'

workflows:
  deploy-over-ssh-workflow:
    jobs:
      - deploy-over-ssh
